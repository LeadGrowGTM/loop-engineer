[GOAL] Four artifacts shipped — execution-mode router, parallel red-team workflow, first-principles directive wired into planner+maker, setup-system diagnosis — committed, checker PASS, synced global.

[DATE] 2026-06-30

[TASK]
Meta-work on this harness repo (root: C:\Users\mitch\Everything_CC\agent-harness). Make the two-prompt loop first-class machinery, add a routing brain, diagnose the setup. Per-phase detail in HARNESS.md — read first.

Stack: Markdown, Claude Code Workflow JS (.claude/workflows/*.js — see ship-change.js), TypeScript/Bun.

Must include:
- references/execution-mode-routing.md — decision layer: single-run vs goal-loop vs time-loop vs dynamic-workflow. First-match-wins order, interval guidance, mode-nesting. Per Anthropic's loops + dynamic-workflows articles.
- .claude/workflows/red-team.js — prompt #2 made real: 4 parallel attack roles (hostile user, careless user, performance, security), each its own angle entry→failure; worst-first merge dedupes across roles. Valid JS, meta.name='red-team', schema-validated findings.
- references/first-principles-generation.md — prompt #1 (derive from constraints, root cause over symptom, reasoning before code), WIRED into harness-planner.md and harness-maker.md. Doc alone is not done.
- docs/setup-system-diagnosis.md — diagnose scripts/setup-harness.ts. Name the prover-not-installed defect at setup-harness.ts:156 (install + smoke omit harness-prover.md; README claims 4 agents), plus duplicated agent lists, unwired seed/patch CLI, missing uninstall, cross-repo install generalization. Prioritized fixes; each claim cites file:line.
- SKILL.md: add an "Execution Mode Routing" section pointing at the new ref + red-team.js.

Drafts of the first two exist — verify and harden, don't trust blindly.
Firstmate: grep found ZERO matches (files + git history). Run `grep -ri firstmate .` + `git log --all -i --grep=firstmate` once, paste empty output to HANDOFF.md as 'nothing to revert' proof. Don't touch history.

Quality bar: production-grade. Repo is canonical; SKILL.md repo copy drifted from global — reconcile, don't blind-overwrite.

Done means:
- 4 files exist; grep each marker (4 mode names; meta.name='red-team'; first-principles directive in planner AND maker; "setup-harness.ts:156" in diagnosis).
- `bun -e "import('./.claude/workflows/red-team.js')"` exits 0.
- HANDOFF.md has the firstmate non-finding with empty grep output pasted.
- Changed skill/agent/workflow files synced repo→global (~/.claude/skills, /workflows, /agents).

[HARNESS]
Read C:\Users\mitch\Everything_CC\agent-harness\temp\goals\harness-execmode-redteam\HARNESS.md first.
1. Planner (turns 1-5): write BRIEF.md + PLAN.md. No artifacts until PLAN.md exists.
2. Maker (turns 6+): execute per PLAN.md (all "direct"), commit each phase, proof to PROGRESS.md.
3. Checker: fresh subagent per CHECKER_BRIEF — paths only. Static goal, skip Prover. Scores to CYCLE_LOG.md.

[EVAL LOOP]
Turn 1: write eval plan in HANDOFF.md, then execute. Up to 3 cycles:
1. Produce/harden output (spec fixed — change only output).
2. Mechanical gate: every Done bullet's grep/parse check passes. Fix + re-run before step 3.
3. Checker (CHECKER_BRIEF) scores 4 dims + reward to CYCLE_LOG.md.
4. Done = gate green AND checker mean ≥3.5/5.0 → commit, finish. Else fix only the lowest dimension, return to step 1.
5. 3 identical rewards → plateau: commit best, note it. Reward = (Done bullets passing)/(total); log each cycle.

[PROOF PROTOCOL] Each phase → PROGRESS.md: phase COMPLETE, artifact path, Proof = pasted command output (grep -c / bun -e exit), Commit SHA. Never "complete" without proof below it.

[BLOCKERS] Hard blocker → stub, note under 'Needs My Decision' in HANDOFF.md, continue unblocked work. Never silently downgrade.

[TURN LIMIT] Stop after 40 turns. Leave HANDOFF.md in repo root regardless (completed, firstmate proof, decisions, evidence). tasks-axi done harness-execmode-redteam on success.
