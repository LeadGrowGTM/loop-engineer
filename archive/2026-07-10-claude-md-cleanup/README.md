# CLAUDE.md cleanup — 2026-07-10

A half-finished restructure had deleted the rich root `CLAUDE.md` (repo map, core
principle, commands) and left a 9-line stub at `.claude/CLAUDE.md`. Restored the
rich root file as canonical (matches `setup-harness.ts`, which patches root, and the
Claude Code default). These two archived files are the superseded stub + an older
backup, kept for reference.
