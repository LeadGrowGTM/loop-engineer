# Goal Brief — harness-execmode-redteam

## Problem
The harness currently has two unverified drafts (execution-mode-routing.md, red-team.js) sitting unwired into the canonical SKILL.md, a first-principles authoring gap in the planner/maker agents, and a setup script with a confirmed, silent defect that leaves the Prover agent uninstalled — so anyone running `/setup-harness` today gets a 3-agent harness even though README.md promises 4.

## Success criteria (product-level)

A future goal author who reads the canonical `skills/write-goal-prompt/SKILL.md` can:
1. Route any task to one of 4 execution shapes (single-run / goal-loop / time-based-loop / dynamic-workflow) using a hardened `references/execution-mode-routing.md` with unambiguous first-match-wins decision order, interval guidance, and explicit mode-nesting rules — cross-checked against the two source Anthropic articles (loops; dynamic workflows).
2. Invoke `.claude/workflows/red-team.js` and get real adversarial verification: `meta.name === 'red-team'`, 4 parallel attack roles (hostile user / careless user / performance / security) each blind to the others, a barrier before merge, worst-first severity-ordered dedup, and schema-validated findings on both the per-role and merged output. The file must import clean under `bun` (syntax-check passes — `bun build --no-bundle` or equivalent; the Workflow-DSL globals `phase`/`parallel`/`agent`/`log`/`args` are injected at runtime by the Workflow executor, not real imports, so "clean" means valid JS syntax, not a standalone run).
3. Trust that Planner (`harness-planner.md`) decomposition and Maker (`harness-maker.md`) reasoning-before-code both explicitly reference a new `references/first-principles-generation.md` directive — not just a doc nobody reads.
4. Read `docs/setup-system-diagnosis.md` and get a real diagnosis with file:line citations, not a vague summary — specifically naming:
   - **Confirmed defect** at `scripts/setup-harness.ts:156` — the `install` command's copy loop only lists `['harness-planner.md', 'harness-maker.md', 'harness-checker.md']`; `harness-prover.md` is never copied to `~/.claude/agents/`, even though the repo's own `.claude/agents/` has all 4 files and README.md documents a 4-agent loop.
   - **Smoke-test gap** at `scripts/setup-harness.ts:106-134` (`smokeTest()`) — hardcodes 3 separate `passed:` checks (lines 113-123) for planner/maker/checker only; no check for harness-prover.md, so a broken install (defect above) still reports all checks green.
   - **Duplicated agent enumeration** — the "which agents ship" list is hand-written independently at line 156 (install copy loop) and lines 113-123 (smoke checks) instead of a single shared constant; this is exactly why they silently drifted out of sync when harness-prover.md was added to the repo's `.claude/agents/` but not to either list.
   - **Unwired CLI commands** — the top-of-file usage comment (lines 6-9) documents `seed <dir> <template-path>` and `patch <claude-md-path> <block-string>` as standalone invocable commands, but the CLI dispatcher (lines 138-183) only handles `scan`, `smoke`, `install`; `seedRoutingTable()` and `patchClaudeMd()` are only reachable indirectly, inside `install`.
   - **Missing uninstall** — no command removes the copied agent files, `.harness/skill-routing.md`, or the CLAUDE.md `## Harness` block; there is no way to cleanly revert an install.
   - **Cross-repo generalization defect** at `scripts/setup-harness.ts:171-172` — the CLAUDE.md `## Harness` block hardcodes the literal string `LeadGrowGTM/loop-engineer` as the source repo name instead of deriving it from `git remote get-url origin`; line 171 also uses the CommonJS `__dirname` global inconsistently with `import.meta.dir` used elsewhere in the same file (lines 153, 161) — both mean the script would misattribute or behave inconsistently if vendored into another repo.
5. See a new "Execution Mode Routing" section in the canonical `SKILL.md` that links to the hardened reference + red-team workflow, and confirm the same section (plus red-team.js, plus the first-principles reference) has been synced to `~/.claude/skills/write-goal-prompt/`, `~/.claude/agents/`, and `~/.claude/workflows/` — closing the exact gap this brief found (global `~/.claude/agents/` already has `harness-prover.md` present from a prior manual workaround, but global `~/.claude/workflows/` has only `ship-change.js` — red-team.js has never been synced).

## Important drift note (verified, not assumed)
The global copy of SKILL.md at `C:\Users\mitch\.claude\skills\write-goal-prompt\SKILL.md` is **not strictly behind** the repo canonical copy — it has DRIFTED IN BOTH DIRECTIONS:
- Global is AHEAD: has an "Execution Router" section (in-session harness vs gnhf vs parallel-gnhf infra choice), tasks-axi wiring, `[TASKS-AXI]` block, gnhf Path section, `disable-model-invocation: true` frontmatter — none of which exist in the repo copy.
- Global is BEHIND: its Phase 1.5 "Agent 4 — Harness Architect" prompt still only confirms 3 agent files (planner/maker/checker) and writes a 3-section HARNESS.md (no PROVER_BRIEF) — the repo copy already has the 4-agent, 5-section version (with PROVER_BRIEF).
- Naming collision risk: global's existing "Execution Router" heading is about infra choice (in-session vs gnhf), a *different axis* from the new "Execution Mode Routing" section this task adds (single-run/goal-loop/time-loop/dynamic-workflow shape). Phase 5 must pick a heading that does not collide with or get confused for global's existing section — do not overwrite it during sync, and consider a one-line cross-reference between the two axes.

## Out of scope
- Do NOT touch gnhf/tasks-axi/treehouse wiring in the global SKILL.md — that content only exists in global and is out of this task's scope; sync must not delete or overwrite it.
- Do NOT alter git history. Firstmate check is read-only (`git log --all -i --grep=firstmate` and `grep -ri firstmate .`), confirm zero matches, record the empty output as proof — never rewrite/revert history on the basis of this check.
- Do NOT implement the setup-harness.ts fixes themselves in this task — `docs/setup-system-diagnosis.md` is a diagnosis deliverable (defects + prioritized fixes), not a patch. Actually fixing setup-harness.ts is future work referenced by, not performed in, this goal.
- Do NOT add a Prover phase to this goal's own harness — this is a static-artifact goal (docs + JS + TS diagnosis, no running app); HARNESS.md already states skip Prover, go straight to Checker.
